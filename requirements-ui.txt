# Run the Streamlit demo UI (ui/app.py): pip install -r requirements-ui.txt
# Deliberately independent of requirements.txt and requirements-api.txt — the
# UI is a thin HTTP client and needs neither the offline pipeline nor the
# API server's own dependencies (FastAPI, psycopg, etc.) to run.

streamlit>=1.35
httpx>=0.27
